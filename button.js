const button = document.createElement('button')
button.innerHTML = "click there now or else it wont be added"
const section = document.getElementById('shopify-section-hero-1')
section.appendChild(button)
